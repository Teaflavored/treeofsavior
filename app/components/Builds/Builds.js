import React, { Component, PropTypes } from "react";

class Builds extends Component {
    render() {
        return (
            <div>
                this is the builds page
            </div>
            );
    }
}

export default Builds;