import React, { Component, PropTypes } from "react"

class ShowSkill extends Component {
    render() {
        return (
            <div>This is the show skill page</div>
        );
    }
}

export default ShowSkill;